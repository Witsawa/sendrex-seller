import template from './publish.html';
import controller from './publish.controller';
import './publish.scss';

let publishComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default publishComponent;
